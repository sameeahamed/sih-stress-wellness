# api-contract.md

Placeholder — intended final content: the frozen REST API contract that both
frontends (Flutter + Next.js) build against.

Planned sections:
- Conventions (JSON, auth, error shape, pagination)
- Endpoint table (method, path, roles, request/response)
- Auth endpoints (login, /me)
- Personnel endpoints (profile, wellness assessment submit, own history,
  own latest recommendation)
- Officer/commander endpoints (dashboard summary, high-risk list, personnel
  risk detail, review workflow)
- Admin endpoint (audit logs)
- Mock JSON samples for mock-first frontend development