# ============================================================
# Environment template — PLACEHOLDER VALUES ONLY
# Copy to `backend/.env` and fill with real values locally.
# ============================================================

# Backend runtime
APP_NAME=SIH 2026 Stress & Wellness Monitoring API
ENVIRONMENT=development
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:3000

# PostgreSQL connection (local dev)
DATABASE_URL=postgresql+psycopg2://CHANGE_ME_USER:CHANGE_ME_PASSWORD@localhost:5432/CHANGE_ME_DB